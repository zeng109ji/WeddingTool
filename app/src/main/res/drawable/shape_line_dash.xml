<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="line" >
    <stroke
        android:dashGap="2dp"
        android:dashWidth="1dp"
        android:width="0.5dp"
        android:color="@color/bg_color_first" />

    <!-- 虚线的高度 -->
    <size android:height="1dp" />
</shape>